# Viewer 旧画面ベースライン (2026-08-07)

実データの値そのものは記載せず、後日の計測結果を数値・実行計画としてのみ追記する。
実在アカウントの screen name 等は本ファイルへ転記しない。

| 画面               | warm TTFB  | cold TTFB  | 初期 HTML サイズ | 主要クエリ EXPLAIN 保存先                           |
| ------------------ | ---------- | ---------- | ---------------- | --------------------------------------------------- |
| `/`                | (計測待ち) | (計測待ち) | (計測待ち)       | `docs/viewer-baseline/explain/dashboard.txt`        |
| `/accounts`        | (計測待ち) | (計測待ち) | (計測待ち)       | `docs/viewer-baseline/explain/accounts.txt`         |
| `/accounts/[id]`   | (計測待ち) | (計測待ち) | (計測待ち)       | `docs/viewer-baseline/explain/account-detail.txt`   |
| `/labels`          | (計測待ち) | (計測待ち) | (計測待ち)       | `docs/viewer-baseline/explain/labels.txt`           |
| `/weekly-runs`     | (計測待ち) | (計測待ち) | (計測待ち)       | `docs/viewer-baseline/explain/weekly-runs.txt`      |
| `/crawl-runs`      | (計測待ち) | (計測待ち) | (計測待ち)       | `docs/viewer-baseline/explain/crawl-runs.txt`       |
| `/crawl-runs/[id]` | (計測待ち) | (計測待ち) | (計測待ち)       | `docs/viewer-baseline/explain/crawl-run-detail.txt` |
| `/block-runs`      | (計測待ち) | (計測待ち) | (計測待ち)       | `docs/viewer-baseline/explain/block-runs.txt`       |
| `/block-runs/[id]` | (計測待ち) | (計測待ち) | (計測待ち)       | `docs/viewer-baseline/explain/block-run-detail.txt` |

## 本番相当テーブル件数

| テーブル             | 件数 (計測待ち) |
| -------------------- | --------------- |
| `Account`            |                 |
| `AccountLabelLatest` |                 |
| `AccountLabel`       |                 |
| `Tweet`              |                 |

計測は運用者が本番相当環境で実施し、このファイルへ数値のみ追記する。
実在データそのもの (screen name、bio、tweet 本文) は記載しない。
