# CLAUDE.md

このリポジトリで作業する際の規約をまとめる。詳細な設計背景は [README.md](./README.md) と [SPEC.md](./SPEC.md) を参照。

## 最重要ルール: 実在する Twitter/X のデータをリポジトリに残さない

このプロジェクトは実在アカウントのクロール結果を扱うため、開発中に実データがソースコードへ紛れ込みやすい。**実在する、特定可能な Twitter/X のデータを、ソースコードのコメント・テスト fixture・ドキュメントに書かないこと。**

禁止対象には次のものが含まれる。

- 実在するスクリーンネーム / ハンドル (`@` 付きかどうかを問わない)
- 実在する表示名
- 実在するツイート / ポストの本文
- 実在するプロフィール文 (bio)
- 実在するプロフィール URL やアカウント URL
- 実在する `t.co` リンク、およびその他の実在する短縮 URL

クロール結果や実際に観測したアカウントからコピーしてきたものは、たとえ一部であっても該当する。

### 代わりに架空の値を使う

検出ルールやヒューリスティックの意図をコメントで説明したい場合、あるいはテストに例示データが必要な場合は、明らかに架空とわかるプレースホルダーを使う。

- ハンドル: `alice`、`bob`、`test_user` のような汎用的なもの
- bio・ツイート本文: 実在の投稿を思わせない、こちらで創作した文章
- URL: 形だけ揃えた架空のもの (例: `https://t.co/exampleXXXX`)

架空の値に置き換える際は、**そのコメントやテストが示そうとしている性質を保つ**ことが重要になる。言語、文字数、キーワードの出現パターン、閾値との大小関係、ルールがマッチすべきかどうか — こうした検証対象の性質は維持したうえで、実在アカウントに辿り着けない値にする。

### 開発中も例外にしない

「まずテストを通したいから実例を貼っておく」という進め方は、そのままコミットされる事故につながる。デバッグ中の一時的な貼り付けであっても、コミット前に必ず架空の値へ置き換えること。

なお、実データを含むディレクトリ (`data/`、`logs/`) は `.gitignore` で除外済みだが、これはあくまでファイル単位の防御であり、コメントや fixture に直接書かれた値は防げない。

## 構成

pnpm workspace (`crawler`、`viewer`) と、その両方が共有する Prisma スキーマ (`prisma/schema.prisma`) からなる。スキーマはリポジトリルート直下に置き、各パッケージからは `--schema=../prisma/schema.prisma` で相対参照する。

- `crawler/`: Twitter のタイムライン・ツイート・プロフィールを収集し、`crawler/labels/rules/` のルール群でラベリングして Postgres に保存する
- `viewer/`: 収集結果を表示する Next.js アプリ

## 言語

- ドキュメント (Markdown)、コードコメント、コミットメッセージ: 日本語
- 日本語と英数字の間には半角スペースを入れる
- 識別子、ログ出力、エラーメッセージ: 英語

## ツールチェイン

Node.js 24 / TypeScript / pnpm 9。パッケージごとに以下のスクリプトが用意されている。

| コマンド | 内容 |
| --- | --- |
| `pnpm lint` | ESLint (`@book000/eslint-config`) |
| `pnpm typecheck` | `tsc --noEmit` |
| `pnpm test` | Vitest |
| `pnpm format` | Prettier (`--check`) |
| `pnpm check` | lint・typecheck・test を並列実行 |

`crawler`・`viewer` のいずれを変更した場合も、そのパッケージで `pnpm check` と `pnpm format` が通ることを確認してからコミットする。CI (`.github/workflows/nodejs-ci.yml`) は両パッケージに対して実行される。

Prettier の設定はセミコロンなし・シングルクォート・`trailingComma: all`・`printWidth: 100`。手で整形せず `prettier --write` に任せる。

## ラベリングルール

`crawler/labels/rules/` の各ルールは、実装ファイルと同名の `.test.ts` を必ず持つ。ルールを追加・変更する際は、マッチすべきケースとマッチすべきでないケースの両方をテストする (特に、部分一致による誤検出を防ぐ境界ケース)。

テストデータは前述のとおりすべて架空の値で構成する。
